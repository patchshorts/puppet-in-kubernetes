apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.pik.service: puppetdb
  name: puppetdb
spec:
  resources:
    requests:
      storage: 100Mi
  accessModes:
    - ReadWriteMany
status: {}
